{
    "version": 2,
    "builds": [
      { "src": "api/**/*.js", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/auth/(.*)", "dest": "/routes/auth.js" },
      { "src": "/webhook/(.*)", "dest": "/routes/auth.js" }
    ]
  }
  